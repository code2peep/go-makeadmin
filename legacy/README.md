# Legacy Sources

`legacy` stores upstream or demo-only source kept for reference.

Rules:

- Do not import files from `legacy` into runtime code.
- Keep upstream paths when moving files here, so future comparisons remain easy.
- Add a short README in each legacy package explaining why it exists.
- Remove a legacy package only after its replacement or deletion decision is documented.
